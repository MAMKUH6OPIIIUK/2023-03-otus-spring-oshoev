spring:
  datasource:
    url: jdbc:h2:mem:library-test
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
  shell:
    interactive:
      enabled: false